[package]
name = "murmur-tool-fixture"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "murmur-tool-fixture"
path = "src/main.rs"

[dependencies]
serde_json = "1"

# Standalone on purpose: this crate is test input, not a member of the murmur
# workspace. Same shape as the graduation fixtures in ../graduation/src/.
[workspace]
